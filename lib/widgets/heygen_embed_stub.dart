import 'package:flutter/material.dart';

// Non-web stub: return an empty box to avoid build errors on mobile/desktop
Widget buildHeygenEmbed(String url, {double? width, double? height}) {
  return const SizedBox.shrink();
}
